{
  "dependencies": {
    "next": "^12.3.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "scripts": {
    "build": "next build",
    "dev": "explorer http://localhost:3000 & next dev",
    "deploy": "cd out && git init && git add . && git commit -am deploy && git branch -m out && git push --force https://github.com/mitsukuri/mitsukuri.github.io out && rm -rf .git && cd ..",
    "export": "next build && next export",
    "lint": "next lint",
    "start": "next start"
  },
  "devDependencies": {
    "@types/node": "18.8.4",
    "@types/react": "18.0.21",
    "typescript": "4.8.4"
  }
}
